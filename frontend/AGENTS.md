# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Use Expo Router's navigation exports (`expo-router/react-navigation` and
`expo-router/js-tabs`) instead of external `@react-navigation/*` imports.
